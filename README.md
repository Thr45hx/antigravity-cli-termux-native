# antigravity-cli-termux-native
Run Google's Antigravity CLI (agy) fully native on Termux (aarch64) — no proot. Segment re-alignment + glibc-loader-direct (no patchelf) + a disposable loader to survive self-updates + cgo-resolver DNS. One-command installer.
